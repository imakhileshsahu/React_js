React  intro
if we use react in browser then we can use reactDOM.
if we are use react in mobile APP then we can use  react -native
.
type is defined which you like(anker,paragraph)
props: it is property like href,target etc
custom render use two thing one was reactElement (what were use) and another that maincontainer(where was use)

In react data can be updated through hooks

Redux is used to state managed in a organised way.
redux version in market used in redux-toolkit(RTK)

context-API search on google read docs file

remember config in change in tailwind 
"""darkMode:"class". """

nanoid is used to generate a unique id
